Menu Default:

```jsx
import { BrowserRouter as Router } from 'react-router-dom';

const props = {
  title: 'MENU',
  links: ['ARTICLES', 'GET STARTED'],
};

<Router>
  <Menu {...props}/>
</Router>
```
